{
  "name": "pipeline-test",
  "version": "1.0.0",
  "description": "Simple Express app to test pipeline",
  "main": "node.js",
  "scripts": {
    "build": "echo 'Building app...' && mkdir -p dist && cp node.js package.json dist/",
    "start": "node node.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Udara",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  }
}
